[table settings]
#不读取的表
ignore list=
#流水表－－不同步删除操作
flow list=




 #延迟加载的表--不会在一开始就加载
#延迟加载--
lazy list=
#延迟加载--处理特殊的表,也就是一张表对应多个player_id,暂时在代码里写死成只支持对应两个player_id
mulcolumn list=

#延迟加载--tb_player的从表A的从表B,A、B表要么一起延迟加载，要么不做。先加载主表再加载从表，先卸载从表再卸载主表。
#暂时规定第一个字段为主表A，第二个字段为表A的主键，第三个字段为表B，第四个字段为表B的主键，第五个字段为set类型
slave list=


